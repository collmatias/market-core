# Build-time dependencies for VetCoreSoft Desktop .exe
# These are NOT needed at runtime — PyInstaller bundles everything.
pyinstaller>=6.0
Pillow>=10.0

# All runtime dependencies (must be installed so PyInstaller can bundle them)
Django==5.2.12
djangorestframework==3.16.1
django-cors-headers==4.9.0
django-sslserver>=0.22
requests==2.32.5
cryptography>=42.0
dbfread==2.0.7
